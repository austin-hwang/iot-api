{
  "name": "datachain_backend",
  "version": "1.0.0",
  "description": "Backend for datachain BoT",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "truffle migrate --reset --compile-all && babel-node index.js --presets es2015"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "js-sha256": "^0.9.0",
    "truffle-contract": "^3.0.5"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
